import { styled } from "../../../../../styled-system/jsx";

const StyledList = styled("ul", {
  base: {
    display: "flex",
    flexDir: "column",
    gap: "4px"
  },
});

export default StyledList;
