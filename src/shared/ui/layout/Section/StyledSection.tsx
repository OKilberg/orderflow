import { styled } from "../../../../../styled-system/jsx";

const StyledSection = styled("section", {
  base: {
    width: 796,
    display: "flex",
    flexDir: "column",
    padding: 18,
    border: "1px solid",
  },
});

export default StyledSection;
